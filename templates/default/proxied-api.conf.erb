server {
  listen 80;
  server_name <%= @domain %>;

  location / {
    proxy_pass http://cluster;
    <% if @forwarded_for %>
    proxy_set_header X-Forwarded-For <%= @forwarded_for %>;
    <% end %>
    proxy_set_header Host "<%= @api_name %>.api.localhost";
  }
}

