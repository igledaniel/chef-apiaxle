{
  "redis": {
    "host": "<%= @redis_host %>",
    "port": <%= @redis_port %>
  },
  "application": {
    "debug": true
  },
  "logging": {
    "level": "<%= @log_level %>",
    "appenders": [{
      "type": "console"
    }]
  },
  "enable_builtin_stats": <%= node[:apiaxle][:proxy_event_subscriber][:enable_builtin_stats] %>,
  "hit_processors": <%= node[:apiaxle][:proxy_event_subscriber][:hit_processors].map do |p|
    {
      path: p['require_path'],
      args: p['args']
    }
  end.to_json %>
}
