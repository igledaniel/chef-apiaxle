{
  "redis": {
    "host": "<%= @redis_host %>",
    "port": <%= @redis_port %>
  },
  "application": {
    "debug": true
  },
  "logging": {
    "level": "DEBUG",
    "appenders": [{
      "type": "stdout"
    }]
  }
}
