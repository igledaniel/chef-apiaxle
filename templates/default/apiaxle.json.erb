{
  "redis": {
    "host": "<%= @redis_host %>",
    "port": <%= @redis_port %>
  },
  "application": {
    "debug": true
  },
  "logging": {
    "level": "<%= @log_level %>",
    "appenders": [{
      "type": "stdout"
    }]
  }
}
